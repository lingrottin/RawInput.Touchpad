<Window x:Class="RawInput.Touchpad.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		x:Name="WindowRoot"
		Title="RawInput Touchpad"
		Height="260" Width="400" ResizeMode="NoResize">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" VerticalAlignment="Center"
				   Margin="10,0">
			<Run Text="Precision touchpad exists:"/>
			<Run FontWeight="Bold"
				 Text="{Binding ElementName=WindowRoot, Path=TouchpadExists}"/>
		</TextBlock>

		<Button Grid.Row="0"
				HorizontalAlignment="Right"
				BorderThickness="0" Padding="8,4"
				Content="Copy to clipboard"
				Click="Copy_Click"/>
		
		<TextBlock Grid.Row="1"
				   Margin="10,10,10,173"
				   TextWrapping="Wrap"
				   Text="{Binding ElementName=WindowRoot, Path=TouchpadContacts}"/>
        <Grid Margin="10,54,10,10" Grid.Row="1">
            <Button x:Name="Start" Content="Start mapping" Margin="10,120,10,10" Click="Start_Click" IsEnabled="{Binding ElementName=WindowRoot, Path=TouchpadExists}"/>
        </Grid>
    </Grid>
</Window>